1) Install splice rpm to grab the rpm dependencies
2) Remove splice rpm
3) cd src/splice && python setup.py develop
4) dev_setup.py -I

Start:
 mongod
 httpd
 rabbitmq-server
 splice_celerdy
 splice_celerybeat

Now try a https fetch
cd playpen/curl_scripts && ./https_client_checkin.sh

